<%# Link to this page with (sanitized) parameters intact to maintain per_page & page %>
<%# Use .csv extension %>
<%= link_to url_for(params: Blacklight::Parameters.sanitize(params.to_unsafe_h), format: 'csv'),
                    class: 'btn btn-secondary',
                    download: BookmarksCsvExportComponent.new(response: @response,
                                                              search_state: search_state).bookmarks_csv_filename do %>
  <i class="fas fa-download"></i> <%= t('blacklight.bookmarks.export_csv') %>
<% end %>
