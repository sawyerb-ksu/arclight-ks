<%# TODO: Refactor this feature into components. %>

<% if collection_count.positive? %>
  <li id="<%= key %>">
    <span class="fw-bold text-muted"><%= key %>. </span>
    <%= link_to search_action_path(f: { ua_record_group_ssim: [subgroup_full_value] }) do %>
      <%= info.dig('title') -%>
    <% end %>
    <span>(<%= number_with_delimiter(collection_count) %>)</span>
    <p class="small"><%= info.dig('description')&.html_safe %></p>
  </li>
<% end %>
